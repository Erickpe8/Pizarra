APP_PORT=8080
VITE_PORT=5173
APP_URL=http://localhost:8080
APP_NAME=Pizarra

DB_DATABASE=pizarra
DB_USERNAME=pizarra
DB_PASSWORD=secret
DB_ROOT_PASSWORD=secret
FORWARD_DB_PORT=3306

# Modo liviano: NO arranca Node/Vite.
# Assets: docker compose --env-file .env.docker --profile frontend run --rm node sh -c "npm ci && npm run build"
# Hot reload: docker compose --env-file .env.docker --profile frontend up -d
